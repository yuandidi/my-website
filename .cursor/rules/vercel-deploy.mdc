---
description: Vercel 部署与 Postgres 运维（环境变量、DB 初始化、CLI 陷阱、生产验证）
globs: apps/web/**/*,lib/**/*,scripts/**/*,vercel.json,package.json,.env.example
alwaysApply: false
---

# Vercel 部署与运维

本仓库生产路径：**Next.js App Router 全栈 + Vercel Postgres（Neon）**。

处理部署、环境变量、数据库初始化或生产 API 故障时，优先遵循本节。

## 架构要点

- 应用：`apps/web`（Next.js 16，`src/app` 页面路由）
- API：单一 Route Handler `src/app/api/[[...path]]/route.ts`，分发至 `lib/api-handlers/*`
- 业务逻辑：`lib/blog.ts`、`lib/db.ts` 等；建表/种子：`lib/setup.ts` + `scripts/`
- 生产域名：`https://dididi.space`（及 `www.dididi.space`）
- Vercel 项目：`yuandidis-projects/my-blog`；**Root Directory = `apps/web`**（见 `apps/web/vercel.json`）

## 环境变量（常见陷阱）

| 变量 | 说明 |
|------|------|
| `POSTGRES_URL` | Vercel Postgres 集成自动注入（生产 Runtime 可用） |
| `DATABASE_URL` | 同上，互为 fallback（见 `lib/db.ts`） |
| `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` | GitHub OAuth |
| `AUTH_SESSION_SECRET` | 会话 Cookie 签名 |
| `GITHUB_DEVELOPER_LOGIN` | 开发者 GitHub 账号 |
| `SITE_URL` | OAuth 回调根 URL（生产 `https://dididi.space`） |

**AI 必须知道的限制：**

1. **`vercel env pull` 不会暴露 Storage 集成变量的真实值**  
   拉下来的 `.env.vercel.production` 中 `POSTGRES_URL=""` 是占位空串，不是解密后的连接串。

2. **空字符串会阻断 fallback**  
   `lib/db.ts` 用 `?.trim()` 处理，避免空 `POSTGRES_URL` 覆盖有效的 `DATABASE_URL`。

3. **关联 Postgres 后必须 Redeploy**  
   新注入的环境变量只在重新部署后的 Runtime 生效；`health` 仍 500 时先触发部署。

4. **生产库初始化只能在本地执行**  
   Vercel Dashboard → Storage → Postgres → 复制 `POSTGRES_URL_NON_POOLING` 到本地 `.env`，再 `pnpm db:setup`。

5. **勿提交** `.env`、`.env.vercel`、`.env.vercel.*`（已在 `.gitignore`）

## 数据库初始化（本地执行）

```bash
# 1. 从 Vercel Dashboard 复制 POSTGRES_URL_NON_POOLING 到 .env
# 2. 建表 + 种子数据
pnpm db:setup
```

## Vercel CLI（查部署与日志）

项目已 link 到 `yuandidis-projects/my-blog`（`apps/web/.vercel/`）。在仓库根目录执行时加 `cd apps/web`，或使用 `pnpm deploy:vercel`（内部同样走 `vercel` CLI）。

```bash
cd apps/web
pnpm dlx vercel@latest whoami                          # 须已登录（首次：pnpm dlx vercel@latest login）
pnpm dlx vercel@latest ls                              # 列表首条 Production 须 ● Ready
pnpm dlx vercel@latest inspect <deployment-url> --logs # 构建日志；末行 status ● Ready
```

- **无需 `VERCEL_TOKEN`**：本机 `vercel login` 后 CLI 使用本地凭证（`~/.vercel`）
- **CI / 无头环境**：须在 Vercel Dashboard → Settings → Tokens 创建 Token，并设置 `VERCEL_TOKEN`
- Dashboard 仅作备用；日常 push 后以 CLI 为准

## 部署与验证 SOP

1. `pnpm check` 通过
2. commit + push（`main` 与 `master` 同步，见 `git-pr.mdc`）
3. **Vercel 部署确认**（`apps/web`）：`pnpm dlx vercel@latest ls` → 最新 **Production** 为 **Ready** → `inspect <url> --logs` 确认 build 无报错（非 Ready 则根据日志修复后重新 push）
4. 等待 CD 完成（若未在步骤 3 已确认），或手动 `pnpm deploy:vercel`
5. **若刚关联/变更 Postgres**：确认已 Redeploy
6. **首次部署或 schema 变更**：本地 `pnpm db:setup`（见上文）
7. 验证生产 API：

| 端点 | 期望 |
|------|------|
| `GET /api/health` | `{"status":"ok","services":{"database":"ok"}}` |
| `GET /api/site-meta` | 200 + 站点元数据 |
| `GET /api/categories/tech/posts` | 200 + 分类文章列表 |
| `GET /admin/posts` | 200，管理页（需开发者登录） |
| `GET /` | 200，首页 |

## 故障排查

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| `health` 500，`POSTGRES_URL is not configured` | 未关联 Postgres 或未 Redeploy | Dashboard 关联 Storage → Redeploy |
| `/api/categories/...` 404 | 旧 Vite + 根 `api/` 部署未更新 | 确认已迁移 Next 并 Redeploy |
| Hobby Function 超限 | 多个独立 API 文件 | 已合并为单一 `[[...path]]` catch-all |
| Vercel 未识别 Next | Root Directory 错误 | 设为 `apps/web` |

**`health` 只做 `SELECT 1`，不能代表业务表已存在。**

## 官方参考

- [Next.js on Vercel](https://vercel.com/docs/frameworks/nextjs)
- [Environment Variables](https://vercel.com/docs/projects/environment-variables)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
