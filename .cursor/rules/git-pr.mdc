---
description: Git 提交与 Pull Request 规范（分支、Commit、PR 模板）
alwaysApply: true
---

# Git 与 PR 规范

## 分支策略

- `main` 为稳定分支，受保护；功能在分支开发后 PR 合并
- 分支命名：`feat/<简述>`、`fix/<简述>`、`refactor/<简述>`、`chore/<简述>`
- 一个分支一个目标；大改动拆成多个小 PR

## Commit 规范（Conventional Commits）

```
<type>(<scope>): <subject>

[optional body]
```

| type | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（不改行为） |
| `test` | 补测试 |
| `docs` | 文档 |
| `chore` | 构建、依赖、配置 |
| `style` | 格式化（无逻辑变更） |

- **scope** 可选：`api`、`web`、`shared`、`docker`
- **subject**：祈使句、小写开头、无句号，≤ 72 字符
- 示例：`feat(api): add tag filter to posts list`

## 任务完成 SOP（默认流程）

功能、部署或重构任务**完成后**，按以下顺序收尾（用户明确说「不要提交」时跳过）：

1. **验证**：`pnpm check` 通过
2. **拆分 commit**：按原子提交原则拆成 1～多个 commit（见上文 type/scope）
3. **推送**：`git push origin main`（若远程默认分支为 `master`，同步 `git push origin main:master`）
4. **确认**：`git status` 干净（除刻意忽略的本地文件）

```bash
git status
git add <按逻辑分组的文件>
git commit -m "feat(vercel): ..."
git push origin main
git push origin main:master   # 远程默认分支为 master 时
```

- PR 场景：push  feature 分支后 `gh pr create`，不在此 SOP 内强推 `main`
- 仅问答/评审、无代码变更：不 commit

## Commit 原则

- **原子提交**：一次 commit 只做一件事，可独立回滚
- **不提交**：`.env`、密钥、`node_modules`、`dist`、本地 IDE 配置
- **任务完成默认提交并 push**：见「任务完成 SOP」
- **不 amend 已推送**的 commit；hook 失败时修问题后新建 commit

## Pull Request

### 标题

与 Conventional Commits 格式一致：`feat(web): add post detail page`

### 描述模板

```markdown
## Summary
- 变更动机与核心改动（1–3 条）

## Test plan
- [ ] `pnpm check`
- [ ] 手动验证：<具体页面或 API>

## Notes
- 破坏性变更、迁移步骤、环境变量变更（如有）
```

### PR 质量要求

- 范围聚焦：Reviewer 能在 15 分钟内理解
- **CI 全绿**：GitHub Actions `CI` workflow 通过后再合并
- 含测试：bug 修复与核心逻辑变更须附带测试（见 `testing.mdc`）
- 契约变更：shared 类型变更须在 PR 中说明前后端影响
- CI 全绿后再请求 Review

## CI / CD

- **CI**：GitHub Actions — `quality`（`pnpm check`）与 `build`（`pnpm build`）并行，见 `.github/workflows/ci.yml`
- **CD**：Vercel 关联 `main` 分支，push 后自动部署；环境变量在 Vercel Dashboard 维护

## 禁止操作

- `git push --force` 到 `main` / `master`
- `git reset --hard`、`git push --force` 除非用户明确要求
- 跳过 hooks（`--no-verify`）除非用户明确要求
- 修改 git config（user.name、user.email 等）

## 合并前检查清单

- [ ] lint / test / build 通过
- [ ] 无调试代码（`console.log`、注释掉的代码块）
- [ ] 数据库 migration 已提交且可重复执行
- [ ] 新增环境变量已在 README 或 PR Notes 中说明
