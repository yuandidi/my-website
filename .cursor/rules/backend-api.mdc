---
description: NestJS API 开发约定（模块结构、Prisma、缓存、DTO、测试）
globs: apps/api/**/*
alwaysApply: false
---

# 后端 API 约定（apps/api）

## 模块结构（关注点分离）

每个业务域一个 NestJS 模块，标准三层：

```
posts/
  posts.module.ts
  posts.controller.ts   # 路由与参数，不含业务逻辑
  posts.service.ts      # 业务逻辑、Prisma 查询、缓存
  posts.service.spec.ts # 核心逻辑单元测试（推荐）
```

跨模块复用放 `src/common/`（mapper、DTO、常量）。**依赖注入**获取 Prisma/Cache，禁止 service 内 `new` 基础设施实例。

## Controller

- 薄控制器：方法体直接 `return this.xxxService.method(...)`
- 查询/入参用 DTO + `class-validator`；全局 `ValidationPipe` 已启用 `transform` + `whitelist`
- REST 语义：GET 读、幂等；资源用 slug/id 路径参数，过滤用 query

```typescript
// ✅
@Get()
findAll(@Query() query: PostsQueryDto) {
  return this.postsService.findAll(query);
}
```

## Service

- 读多写少场景用 `CacheService.wrap(key, ttlSeconds, fn)`；写操作需考虑缓存失效
- 列表返回 `PaginatedResponse<T>`（来自 `@my-blog/shared`）
- 资源不存在抛 `NotFoundException`；业务规则违反抛对应 HTTP 异常，勿返回 `null`
- 复杂查询拆私有方法（如 `buildListWhere`），保持公有方法可读

## Prisma 与 Mapper

- 查询条件、include 常量化（`publishedPostWhere`、`postInclude`）
- 公开 API 仅返回 `PUBLISHED` 且 `publishedAt <= now` 的文章
- Prisma `Date` → ISO string 统一在 mapper 完成，输出对齐 shared 类型
- Schema 变更：`pnpm db:migrate`；seed 保持 idempotent；禁止手改已部署 migration

## 缓存

- 列表 key 含查询参数 hash；详情 key 用稳定标识（`posts:slug:${slug}`）
- TTL：列表 300s、详情 600s；新增写接口时同步规划失效策略

## 测试与质量

- 单元 / E2E 写法、Mock 策略见 `testing.mdc`
- 空 catch 禁止；预期外错误向上抛，由 Nest 异常过滤器统一处理

## 禁止

- Controller 内直接 Prisma 查询
- 重复定义与 shared 冲突的 response 类型
- 跳过 DTO 使用裸 `@Query()` / `@Body()`
- 在日志或响应中暴露数据库错误详情
